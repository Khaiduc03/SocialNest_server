export type TypeRole = Array<number>;

export enum UserRole {
    User = 'user',
    Admin = 'admin',
}

export enum Gender {
    famale = 'famale',
    male = 'male',
}

export enum newsStatus {
    Public = 'public',
    Private = 'private',
}
